BUTTON

class GUI_main(App):
        def build(self):
                   return Button(text="SUBMIT",font_size ="30sp", 
                   background_color =(1, 1, 1, 1), 
                   color =(50, 50, 50, 50),  
                   size =(15, 15), 
                   size_hint =(.2, .2), 
                   pos =(400, 250))


BOX LAYOUT
class GUI_main(App):
        def build(self):
  
            superBox        = BoxLayout(orientation='vertical')

            horizontalBox   = BoxLayout(orientation='horizontal')
            button1         = Button(text="One")
            button2         = Button(text="Two")

            horizontalBox.add_widget(button1)
            horizontalBox.add_widget(button2)

            verticalBox     = BoxLayout(orientation='vertical')
            button3         = Button(text="Three")
            button4         = Button(text="Four")

            verticalBox.add_widget(button3)
            verticalBox.add_widget(button4)

            superBox.add_widget(horizontalBox)
            superBox.add_widget(verticalBox)


            return superBox
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
            def build(self):
        #PAGE MANAGER
        #MAIN BOX
            TOP_BOX = BoxLayout(orientation='vertical', spacing=0)
            B_BOX   = BoxLayout(orientation='vertical', spacing=30)
            
        #buttons time and height
            B_height = Button(text = "Drop height", size_hint=(1,.7))
    
            B_time   = Button(text = "Time interval", size_hint=(1,.7))
        #add buttons to B_BOX
            B_BOX.add_widget(B_height)
            B_BOX.add_widget(B_time)
            
            
            TOP_BOX.add_widget(B_BOX)
            
            return TOP_BOX

